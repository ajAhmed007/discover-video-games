//importing the Message component from src
import Message from "./Message"
function App(){
  return <div>
    {/* to use the Message component, 
    we can use call it with self closing tag, like below */}
    {/* using the Message component below */}
    <Message/>

  </div>
}

export default App